# 卡通农场货仓助手

面向《卡通农场》中国大陆版本的网页库存规划工具，用于记录货仓与粮仓库存、设置目标库存、展开生产链需求，并安排设备和基础原料的生产顺序。

## 功能

- 库存记录：维护当前数量、目标数量和隐藏状态。
- 缺货优先级：综合目标差额、下游需求和生产成本辅助排序。
- 生产计划：递归展开产品需求，并先扣除库存中可供下游使用的余量。
- 生产完成：增加成品库存，同时按配方扣除原料；原料不足时不会误记完成。
- 生产链查看：展示产品、原料、设备、生产时间和上下游用途。
- 数据编辑：可修改名称、设备、原材料、生产时间和目标等字段。
- 数据校对：集中查看用户修改、缺失图片和核对状态，并为物品上传本地图片。
- 自定义排序：可拖动生产设备及设备内产品，并保留个人排列习惯。
- 数据保存：浏览器自动保存使用状态，内置服务将可持久化数据写入 `data.json`。

## 运行

```bash
npm install
npm start
```

然后打开：

```text
http://localhost:8766
```

也可以直接打开 `index.html` 预览页面。直接打开和 `localhost` 使用不同的浏览器存储空间；长期使用和保存数据时，请统一通过 `npm start` 启动服务。

## 校验

```bash
npm run test:data
npm run test:core
npm run test:server
npm run test:ui
```

第一次运行 UI 测试时，可能需要安装 Playwright 浏览器：

```bash
npx playwright install chromium
```

## 数据状态

- 内置基础数据包含 465 个物品和 57 个生产分组；应用保存的修改后，当前实际生效 460 个物品。
- 物品、设备和配方以中国大陆版本的中文名称维护。
- 部分水果、鱼肉、礼券等非设备生产物会以 `0s` 或基础资源形式存在。
- `wiki_products.json` 仅作为交叉校验参考，不会自动覆盖用户保存的数据。
- 游戏版本、活动内容和中文译名可能变化，关键数据仍建议结合游戏内信息复核；发现差异时可直接在网页中修改并保存。

## 目录

- `index.html`：主应用。
- `planner-core.js`：库存需求与配方关系的纯计算模块。
- `catalog-migration.js`：目录 ID、浏览器数据与旧备份的一次性兼容迁移。
- `catalog-id-image-mapping.json`：保留的人工确认审计记录，不参与网页日常运行，也不通过本地服务器公开。
- `data.json`：当前库存与生产数据。
- `icons/`：物品和设备图标。
- `server.js`：静态服务和保存接口。
- `validate.py`：数据一致性校验脚本。
- `wiki_products.json`：生产数据参考文件。
- `tests/`：浏览器冒烟测试。

## 版本记录

### v2.25.1

- 收紧内置图片迁移：只有浏览器中的金色礼券、花生图片与已内置的两张测试图片 SHA-256 指纹完全一致时才会清理；同名但内容不同的用户图片保持不变。
- 本地图片库读取或删除失败时跳过本次迁移并继续启动库存助手，不写入完成标记，以便下次启动安全重试。

### v2.25.0

- 修复库存清单下所有物品分组按钮只改变文字和状态、但内容没有实际隐藏的问题，并增加收起与再次展开的真实可见性回归检查。
- 将 465 个内置物品及新增、编辑、旧备份中的历史 Emoji 统一为 `📦`，移除编辑框中的备用图标输入项；不改变物品 ID、配方、时间、库存、目标或图片。
- 将用户测试上传的金色礼券和花生图片原样转为内置资源；新版首次打开时仅从浏览器图片库清理这两条重复记录，其他用户图片和物品数据保持不变。
- 保持 v4 备份结构不变；重新导出的物品编辑统一使用箱子图标，内置图片不进入用户图片备份。

### v2.24.0

- 使用用户确认的 `Golden_Voucher` 图片替换金色礼券占位图，并沿用其他礼券的本地图片尺寸与格式规则。
- 删除已经完成使命的目录对照网页、内嵌快照及一次性目录生成、下载和审计脚本，同时撤销这些临时资源的本地服务器访问入口。
- 保留人工确认的映射审计记录、正式 ID 迁移、旧备份兼容、用户上传图片迁移以及全部已安装本地图标；不改写用户 `data.json`。
- 增加金色礼券真实图片识别和临时目录资源不可访问的回归检查。

### v2.23.0

- GitHub Pages 上没有本地数据的新访客默认看到粮仓与货仓容量各 `8000`，两边库存合计也各为 `8000`。
- 同仓物品采用最均匀的整数分配，任意两种物品数量最多相差 `1`；当前粮仓为 `135/136`，货仓为 `19/20`。
- 展示库存只在静态网页首次访问且浏览器没有本地数据时生成；已有浏览器库存、本地服务数据、用户修改、备份和图片均不会被覆盖。

### v2.22.0

- 根据人工确认的 424 个物品和 51 个设备映射，规范 81 个物品 ID 与 5 个设备 ID，并补入捕鸭器、捕虾笼、神奇渔网、小鸭沙龙、龙虾池和矿场。
- 将 475 张确认图片下载、转换并压缩为本地 PNG；用户上传图片继续保存在浏览器独立图片库并优先显示。
- 启动时原子迁移库存、物品编辑、核对状态、排序、恢复记录、待同步数据和用户图片编号；不直接改写现有 `data.json`。
- 备份格式升级至 v4，增加 `catalogIdVersion`；v3 备份导入时自动换号，图片和用户修正的配方、数量、生产时间保持不变。
- 暂时保留目录对照工作台及迁移审计脚本，并单独记录其文件范围、用途和后续安全删除边界。

### v2.21.0

- 新增本地“物品修改恢复”记录：编辑、新增、删除或导入物品数据前自动保留最多 20 个恢复点，并在恢复前再次自动保护当前状态。
- 恢复操作只回退物品名称、设备、配方、生产时间、目标库存、新增/删除及核对状态；当前库存数量与用户图片保持不变。
- 恢复记录仅保存在当前浏览器，不进入服务器同步，也不改变 v3 备份及原有导入导出结构；存储失败时会停止破坏性操作并提示先备份。
- 关系网改为鼠标右键拖动平移并屏蔽图内浏览器右键菜单；左键拖动不再误清除已选物品，触屏继续使用单指拖动。
- 补充修改恢复、库存与图片隔离、备份兼容、导入删除保护、左右键拖动及右键菜单回归测试。

### v2.20.0

- 新增关系网需求模拟：输入目标数量后递归计算库存覆盖、生产批次、逐层材料、待补缺口、涉及设备与近似关键链，并在完整关系网中高亮相关路径。
- 关系网各辅助模块可独立收起，新增低高度排列；收起后释放的纵向空间会自动补给关系图，便于大屏和移动端查看更多节点。
- 全局统一展开/收起按钮的外形、汉字、触控尺寸和无障碍状态；新增同类按钮会由回归测试自动检查统一组件规则。
- 加强本地服务同步保护：保存请求携带数据版本，旧页面或并发修改不能静默覆盖较新的用户数据；冲突时保留待同步内容并提示先导出备份。
- 保持备份格式 v3 与原有导入导出结构不变，补充需求计算、同步冲突、关系网布局及桌面端和移动端回归测试。

### v2.19.0

- 新增数据校对中心，集中筛选缺少图片、修改待核对和用户已核对物品，不增加新的主导航页签。
- 支持上传 PNG、JPG、WebP 物品图片；图片自动缩放并独立保存在浏览器图片库，不改写库存、配方或人工修改。
- 识别空白内置占位图，避免把占位资源误计为真实图片，并在列表、详情和编辑框中明确提示。
- 备份格式升级至 v3，仅新增可选的 `itemImages`；旧版备份继续兼容且不会清空现有图片。
- 图片恢复采用非破坏合并，旧备份不能覆盖更新的本地图片；导入前会先校验文本和图片字段结构。
- 增加图片存储、备份兼容、占位图识别、桌面端和移动端数据校对回归测试。

### v2.18.1

- 将关系网全屏定义为专注查看模式，隐藏“编辑物品”入口，避免编辑对话框被原生全屏层级遮挡。
- 全屏期间拦截其他路径触发的物品编辑并提示先退出；退出全屏后编辑入口和原有编辑流程立即恢复。
- 增加全屏隐藏编辑入口、阻止后台弹窗和 Esc 恢复编辑入口的回归测试。

### v2.18.0

- 移除关系网桌面端 840px 高度上限，按浏览器可用高度自动铺满并重新适配全图，大屏不再沿用小屏缩放比例。
- 新增关系网全屏按钮，支持再次点击或按 Esc 退出；浏览器不开放原生全屏时自动使用占满视口的兼容模式。
- 全屏时保留搜索、缩放、图例和选中物品详情，并隐藏图外单列区域，把可用空间优先留给关系网。
- 扩充 2560×1440 大屏、全屏状态、Esc 恢复和移动端无溢出回归测试。

### v2.17.0

- 为关系网区分“全景”和“聚焦”两套阅读尺度：全景保留数百个物品的完整结构，聚焦提高桌面端和移动端的可读倍率。
- 将选中物品详情移到关系图上方，并直接显示当前配方与数量；移动端搜索后无需向下滚动即可确认选中内容。
- 全景低缩放时隐藏密集名称，仅保留节点结构和搜索高亮；聚焦时恢复当前上下游名称与数量线标。
- 补充关系节点键盘导航、按钮无障碍名称与移动端触控尺寸，并收拢设备筛选、购买建议、困难排行和统计摘要的首屏高度。
- 扩充关系网完整性、编辑后即时重算、全景返回、聚焦详情及桌面端/移动端显示回归测试。

### v2.16.0

- 将关系页改为单张由左到右展开的全景关系网，一次呈现所有最终产物与完整原料层级，不再按生产设备拆分查找。
- 支持全图适配、缩放、平移、搜索高亮与关系链聚焦；搜索不会隐藏其他节点。
- 物品配方编辑并保存后会立即重算最终产物分类、原料层级和连线数量，当前关系图同步更新。
- 校正鱼饵与渔网：红色鱼饵无原料，其他鱼饵各需一张同色礼券，渔网无原料。
- 更新关系网布局与桌面端、移动端交互测试，标题和包版本更新为 `v2.16.0`。

### v2.15.0

- 在库存助手同一网页新增“关系网”页签，按生产设备纵览所有最终产物。
- 最终产物可逐个或一键展开，从左到右追溯中间产物、动物饲料、农作物与特殊来源。
- 数量以“每份需”、“每批需”等语义文案写在关系线上，并标注饲料批次产量与可选捕鱼路线。
- 无配方且未被其他产品使用的工具、扩建材料和礼券单独列出。
- 新增关系网分析、树形展开与桌面端、移动端交互测试。
- 标题和包版本更新为 `v2.15.0`。

### v2.14.0

- 修复自定义物品备份恢复时库存数量丢失的问题，新增带版本信息的备份格式并继续兼容旧备份。
- 服务端同步失败后保留待同步数据并自动重试，避免浏览器状态与保存文件静默分叉。
- 收紧静态资源访问范围，并在保存数据不可解析时拒绝覆盖，保护备份与项目元数据。
- 生产计划按显示顺序预留共享原料，避免多个任务重复占用同一批库存。
- 将动物及基础原料拆分为独立来源，改善并行生产时间与优先级判断。
- 历史数据迁移仅修正名称和设备，不再覆盖人工核对的生产时间与配方。
- 数据校验将外部资料差异标记为参考提醒，人工修正始终优先保留。
- 增加备份恢复、同步重试、静态资源边界、历史迁移和共享库存的自动化测试。
- 标题和包版本更新为 `v2.14.0`。

### v2.13.0

- 搜索状态增加结果数量和一键清除按钮，避免恢复旧搜索后误以为物品缺失。
- 移动端默认保留常用设备筛选，并通过“更多设备”按需展开完整列表。
- 修复窄屏横向溢出，扩大顶部操作按钮的触控区域。
- 在移动端保留可见的物品编辑入口，并提高编辑对话框辅助标题的可读性。
- 增加搜索反馈、设备折叠和移动端宽度的自动化测试。
- 标题和包版本更新为 `v2.13.0`。

### v2.12.0

- 按中国大陆版本将“保鲜站”更名为“腌制食品铺”。
- 将“花生奶昔”调整到冰淇淋机，并补充人工核对后的原料和生产时间。
- 修正“炸鱼薯条”的产品 ID 与设备归属，将其调整到烧烤架并补充人工核对参数。
- 在油炸摊新增独立产品“手抓酥皮派”（`hand_pies`），避免与“炸鱼薯条”共用产品 ID。
- 增加旧版浏览器数据迁移，保留升级后的设备归属和产品编辑能力。
- 标题和包版本更新为 `v2.12.0`。

### v2.11.0

- 重构缺货优先级：综合安全库存缺口、整条生产链需求和下游配方覆盖面，突出对生产链影响大且库存不足的原料。
- 生产计划增加在线与离线安排方式；在线模式优先可立即开工的短时任务，离线模式按用户设置的时间窗口优先安排长时产品。
- 将物品清单恢复到右侧栏标签下方，并保持粮仓、货仓两栏显示，便于核对人工修正的数据。
- 重做生产链层级展示：同级原料对齐、后续原料逐级缩进，并为未知物品及循环依赖提供明确提示，不再出现 `undefined`。
- 增加优先级、生产模式持久化、物品清单布局和生产链层级的自动化测试。
- 标题和包版本更新为 `v2.11.0`。

### v2.10.0

- 根据全量生产链的原材料需求量调整生产设备默认顺序，让常用设备更靠前。
- 增加一次性排序迁移，现有用户升级后应用新顺序，后续手动拖动仍会永久保留。
- 支持在每个生产设备内拖动产品卡片自定义顺序；搜索、筛选、刷新、备份和导入均兼容个人排序。
- 按中国大陆版本更新设备名称：欧姆蛋小摊、鱼饵机、织网机、华夫饼摊、制陶室、奶昔店和糖果机。
- 增加产品拖动及刷新后持久化的 UI 自动化测试。
- 标题和包版本更新为 `v2.10.0`。

### v2.9.0

- 整理桌面与移动端工作区视觉层级，收紧工具栏、概览、卡片和编辑对话框排版。
- 新增“粮仓”筛选，将耕地与树木产物合并展示，并兼容旧版筛选与排序状态。
- 修复设备筛选按钮与内容分组错位、残留搜索条件遮蔽筛选结果等问题。
- 调整购买建议与最难生产区域；最难生产展开后显示前 12 项。
- 修复可可果迁移数据乱码，并自动清理浏览器中已有的损坏覆盖。
- 为生产设备与原料搜索增加中文输入法感知的浮动候选菜单；汉字上屏后自动展开 6 行候选，拼音组词期间不打断输入。
- 标题和包版本更新为 `v2.9.0`。

### v2.8.0

- 修正递归需求计算：下游原料需求会先抵扣超过目标的现有库存，避免重复安排生产。
- 完成生产时同步校验并扣除配方原料，原料不足时给出阻止提示。
- 将耕地、树木和动物原料纳入生产计划，同时排除无法生产的工具类物品。
- 加强数据编辑校验，阻止循环配方、重复原料、无效自定义编号和仍被配方引用的删除操作。
- 抽离 `planner-core.js` 纯计算模块，新增核心算法与服务端自动化测试。
- 改进服务端保存顺序、原子写入、请求校验、同源限制和静态路径安全。
- 修复页面结构、重复初始化、无效调用和用户可编辑文本的转义问题。
- 完成 macOS Chrome 实机检查，覆盖搜索、生产计划、物品清单和编辑弹窗。
- 标题和包版本更新为 `v2.8.0`。

### v2.7.6

- 精简公开仓库内容，移除辅助脚本和重复锁文件。
- README 改为面向用户和项目维护者的说明。
- 清理 `.gitignore`，避免出现与 Git 内部目录相关的误导性规则。
- 标题更新为 `卡通农场 · 货仓助手 v2.7.6`。

### v2.7.5

- 补齐网页图标资源，避免页面引用缺失。
- 修复服务路径，移除对固定绝对路径的依赖。
- 增加数据校验和 UI 冒烟测试。
- 标题更新为 `卡通农场 · 货仓助手 v2.7.5`。

### v2.7.4

- 成熟基础版本。
